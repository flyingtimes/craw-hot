# Craw-Hot 优化记录

## 优化时间
2026-03-01 12:00

---

## 优化目标

根据日志分析，craw-hot 在抓取过程中存在以下性能问题：

1. **无效滚动浪费大量时间**：某些用户（如 @AIatMeta, @GoogleDeepMind 等）连续滚动 10 次，即使早就发现没有新帖
2. **"无新帖"检测不够及时**：当前逻辑是连续 3 次无新帖才停止，但实际上 2 次就可以确定了
3. **没有"看到昨天的帖子就停止"的功能**：有些用户的第一条帖子就是昨天的，但仍然滚动完 10 次

---

## 优化内容

### 1. 降低无新帖阈值（从 3 次改为 2 次）

**修改位置**：`Config.scroll_no_new_threshold`

```python
# 优化前
scroll_no_new_threshold: int = 3

# 优化后
scroll_no_new_threshold: int = 2
```

**预期效果**：
- 对没有新帖的用户，节省约 30-45 秒
- 不影响准确性，因为连续 2 次无新帖已经足够确定

---

### 2. 添加"检测到昨天的帖子就立即停止"功能

**新增配置**：`Config.scroll_early_stop_on_yesterday`

```python
# 新增配置
scroll_early_stop_on_yesterday: bool = True  # 检测到昨天的帖子就立即停止
```

**实现逻辑**：
在 `_scroll_and_collect` 方法中添加了时间戳检测：

```python
# 在第一次滚动后，检测第一个帖子的时间戳
if self.config.scroll_early_stop_on_yesterday and scroll_num == 1 and urls:
    first_url = urls[0]
    tweet_id = first_url.split('/status/')[-1]
    if tweet_id:
        tweet_timestamp = int(tweet_id)
        current_ts = time.time() * 1000
        one_day_ago = current_ts - (24 * 60 * 60 * 1000)
        if tweet_timestamp < one_day_ago:
            self.logger.info("Detected yesterday's post, stopping immediately")
            found_yesterday = True
            break
```

**预期效果**：
- 对于第一条帖子就是昨天的用户，立即停止（节省 20-30 秒）
- 不影响有新帖的正常抓取

---

## 优化效果预测

| 指标 | 优化前 | 优化后 | 改进 |
|------|--------|--------|------|
| 无新帖用户平均耗时 | ~28s | ~8-12s | ↓ 60% |
| 总抓取时间（19 用户） | 5m13s | ~3m30s-4m | ↓ 20-25% |
| 无效滚动次数 | ~180次 | ~80次 | ↓ 55% |
| 总节省时间 | - | 1-2 分钟 | - |

---

## 受影响的用户

根据日志分析，以下用户将受益最多：

### ⚡ 高优先级受益（节省 20-30 秒）
- @AIatMeta
- @GoogleDeepMind
- @ModdedQuad
- @NSF

### 🟡 中优先级受益（节省 10-20 秒）
- @EHuanglu（部分情况）
- @aigclink（部分情况）
- @geekbb（部分情况）

---

## 验证方法

优化后可以通过以下方式验证效果：

1. **运行抓取**：
   ```bash
   cd /Users/clark/clawd/skills/craw-hot/scripts
   python3 craw_hot.py crawl
   ```

2. **查看日志**：
   ```bash
   tail -f craw_hot.log
   ```

3. **检查优化日志**：
   - 看到 "No new posts for 2 scrolls, stopping early" 说明第一个优化生效
   - 看到 "Detected yesterday's post, stopping immediately" 说明第二个优化生效

---

## 后续可优化点

### 低优先级（可选）
1. **动态调整滚动次数**：根据首次 scroll 结果动态调整最大滚动次数
2. **更精准的时间戳检测**：使用 DOM 获取准确时间，而不是从 URL 解析 Snowflake ID
3. **缓存用户状态**：记住哪些用户经常无新帖，直接跳过或减少滚动次数

---

## 回滚方法

如果优化出现问题，可以回滚：

1. **编辑配置**：
   ```python
   scroll_no_new_threshold: int = 3  # 改回 3
   scroll_early_stop_on_yesterday: bool = False  # 禁用新功能
   ```

2. **或者恢复原始文件**：
   ```bash
   git checkout HEAD -- craw_hot.py
   ```

---

**备注**：本次优化完全向后兼容，只是让抓取更快，不影响功能准确性。
